# 依赖
node_modules/

# 打包产物（node tools/package.js 生成，含 268 MB Electron 运行时）
dist/

# 运行日志
.logs/
logs/
*.log

# tencent-docx 流水线留痕（中间产物，成品已归档到 docs/）
output/8e349cee-ff8f-42c0-aef2-f1dbc87f2edf/

# 自检报告：含本机真实探测结果（装了哪个模拟器、adb 绝对路径），且每次运行都变
output/selfcheck/report.json
# 注：output/selfcheck/*.png 有意纳入版本库 —— README 直接引用它们，
# 不另存副本（副本会漂，正是本项目的硬约定之一）。
# tools/selfcheck/main.js 里钉死了 disable-lcd-text + force-color-profile=srgb，
# 截图字节可重现：同样的 fixture 重跑两次 sha256 全等。
# 所以这几个 PNG 一旦显示为已修改，就是**真的界面变了**，应当看 diff 并一起提交。

# 本地工作记忆（工具侧约定与日志，不进仓库）
.workbuddy/

# 系统与编辑器
Thumbs.db
Desktop.ini
.DS_Store
.idea/
.vscode/
*.swp
